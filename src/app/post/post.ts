export interface Post {
  tagged_user: Array<any>;
  images: Array<any>;
  dislike_cnt: Array<any>;
  like_cnt: Array<any>;
  _id: string;
  user: object;
  body: string;
  created_date: string;
  is_public: boolean;
  is_vissible: boolean;
  perfor_code: string;
  is_thanks: boolean;
  deadline: string;
}
